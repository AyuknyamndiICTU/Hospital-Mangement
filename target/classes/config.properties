# ═══════════════════════════════════════════
# Health Assistance System — Configuration
# ═══════════════════════════════════════════

# Application
app.name=Health Assistance System
app.version=1.0.0

# Database (MySQL)
db.url=jdbc:mysql://localhost:3307/health_assist?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=admin237
db.pool.min=3
db.pool.max=10

# Auto-create database on startup
db.auto.init=true

# Default Admin Credentials (used by AdminSeeder on first run)
admin.default.email=admin@health.com
admin.default.password=Admin@123
admin.default.name=System Administrator
